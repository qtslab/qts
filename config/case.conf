# QTS / AE-QTS 執行參數
# 格式：key = value，'#' 之後為註解。
# 修改後直接重新執行即可生效，不需重新編譯。

N = 64              # Neighbourhood size（鄰域大小）
min_value = 1.0     # 物品價值下界（目前 case 未直接使用，保留供擴充）
max_value = 10.0    # 物品價值上界（目前 case 未直接使用，保留供擴充）
min_weight = 1.0    # 物品重量下界（case_I 使用）
max_weight = 10.0   # 物品重量上界（case_I 使用）
max_gen = 1000      # 迭代代數 NumIter
question_size = 1000 # 物品數量 n_items
test_times = 1      # 重複執行次數（取平均）
